import {IData, IQuantity, ITag} from '@browseth/internal-types/jsonrpc-types';

export function hex(strings: TemplateStringsArray, exp: IData) {
  return;
}

export function quantity(strings: TemplateStringsArray, exp: IQuantity) {
  if (typeof exp === 'number') {
    if (!isNaN(exp) && isFinite(exp) && exp >= 0) {
      return '0x' + exp.toString(16);
    }
    throw new TypeError('must be an unsigned finite number or string');
  }
  if (/^\d+$/.test(exp)) {
  }
}

export function tag(strings: TemplateStringsArray) {}

const hex = (i: IData) => hex`0x${i}`;
