import {OData, IData} from './jsonrpc-types';

export interface SingerWareWallet {
  getAccounts(): Promise<OData[]>;
  signTransaction(): Promise<OData>;
  signMessage(msg: IData): Promise<OData>;
}

export interface FullWallet {
  getAccounts(): Promise<OData[]>;
  sendTransaction(): Promise<OData>;
  signMessage(msg: IData): Promise<OData>;
}
